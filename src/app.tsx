import * as React from 'react'
import ReactDOM from 'react-dom'
import { Landing } from './landing/landing'

import './app.css'

ReactDOM.render(React.createElement(Landing, null), document.getElementById('root'))
